### Android ###
# Built application files
*.apk
*.ap_
*.aab
*.aar
output-metadata.json # Build output metadata file
app/release/output.json # Common pattern for release output files
output.json # General pattern for JSON output files

# Files for the Dalvik VM
*.dex

# Java class files
*.class

# Log/OS Files
*.log
.DS_Store # macOS specific
Thumbs.db # Windows specific
ehthumbs.db # Windows specific
ehthumbs_vista.db # Windows specific

# Generated files / Build directories
bin/
gen/
out/
build/
.gradle/
.externalNativeBuild/
.cxx/
obj/ # Object files for NDK
*.so # Shared library files from NDK

# Local configuration file (sdk path, etc)
local.properties

# Android Studio generated files and folders
captures/
.android/avd/ # AVD (Android Virtual Device) files
.coroutines_debug/ # Kotlin Coroutines debug files
kspCaches/ # KSP (Kotlin Symbol Processing) generated files
.navigation/ # Android Studio Navigation Editor temp files

# IntelliJ / Android Studio IDE files
.idea/
# Ignore specific workspace files to avoid conflicts among developers
.idea/workspace.xml
.idea/tasks.xml
.idea/assetWizardSettings.xml
.idea/gradle.xml # Optional: some teams prefer to include this if Gradle configuration is very specific and needs to be shared
.idea/caches/
.idea/codeStyles/
.idea/libraries/
.idea/vcs.xml # Generally preferred to ignore this
.idea/shelf/ # Changes stashed by the IDE
.idea/navigationEditor.xml # Changes in the Navigation Editor (might be useful to share if the team uses it)
# Specific IDE files generated by Android Studio
.idea/misc.xml
.idea/modules.xml
.idea/deploymentTargetDropDown.xml
.idea/render.experimental.xml
.idea/dictionaries/ # If you don't use shared IDE dictionaries.
# Specific Kotlin cache directories (apart from .kotlin/)
.kotlin/

*.iml # IntelliJ/Android Studio module files
.project # Eclipse project file (legacy)
.settings/ # Eclipse configuration directory (legacy)
.vscode/ # VS Code configuration directory (if used)

# Keystore files (VERY IMPORTANT - DO NOT COMMIT!)
*.jks
*.keystore
keystore.properties # If you have a properties file for your keystore

# Google Services (e.g. APIs or Firebase)
# google-services.json # If you have sensitive API keys, it's safer not to commit this.
                       # Consider sharing it securely or injecting it via CI/CD.

# Crashlytics/Firebase-related files
firebase/
crashlytics-build.properties

# Android Profiling
*.hprof # Heap dump files from the profiler

# Proguard / R8 (Output or temporary directories, not the rule files)
proguard/

# Gradle Wrapper (important to commit these)
#gradle-wrapper.jar # <-- NOTE: this should NOT be ignored, it's needed for others to build the project

# Misc files and directories
plugins/buildsystem/bin # Build system plugin binaries
.stg/ # Stash files from some IDEs
.vagrant/
.vagrantignore